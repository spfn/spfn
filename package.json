{
  "name": "spfn",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "dotenv-cli": "^10.0.0",
    "prettier": "^3.2.5",
    "turbo": "^2.5.8",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@9.1.2",
  "engines": {
    "node": ">=18.18.0",
    "pnpm": ">=8.0.0"
  }
}