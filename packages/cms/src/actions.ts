/**
 * @spfn/cms/actions
 *
 * Server Actions
 * 서버/클라이언트 컴포넌트 양쪽에서 사용 가능한 Server Actions
 *
 * Note: Locale constants (LOCALE_COOKIE_KEY, etc.) are exported from @spfn/cms (index.ts)
 */

// Locale Server Actions
export * from './lib/core/locale.actions';