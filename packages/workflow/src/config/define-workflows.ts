/**
 * Workflow Configuration
 *
 * Re-exports from workflow-router for backwards compatibility during migration
 */

export {
    defineWorkflowRouter,
    isWorkflowRouter,
    type WorkflowRouter,
    type WorkflowRouterConfig,
} from './workflow-router';
