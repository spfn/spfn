/**
 * {{PASCAL_NAME}} Contracts
 *
 * Auto-generated by SPFN CLI
 */

import { Type } from '@sinclair/typebox';
import { createContract } from '@spfn/core/route';

/**
 * GET /{{ENTITY_NAME}}
 * List {{PASCAL_NAME}} items
 */
export const list{{PASCAL_NAME}}Contract = createContract({
    method: 'GET',
    path: '/{{ENTITY_NAME}}',
    query: Type.Object({
        limit: Type.Optional(Type.Number({ minimum: 1, maximum: 100 })),
        offset: Type.Optional(Type.Number({ minimum: 0 })),
    }),
    responses: {
        200: Type.Object({
            items: Type.Array(
                Type.Object({
                    id: Type.Number(),
                    name: Type.String(),
                    description: Type.Union([Type.String(), Type.Null()]),
                    createdAt: Type.String(),
                    updatedAt: Type.String(),
                })
            ),
            limit: Type.Number(),
            offset: Type.Number(),
        }),
    },
});

/**
 * POST /{{ENTITY_NAME}}
 * Create new {{PASCAL_NAME}}
 */
export const create{{PASCAL_NAME}}Contract = createContract({
    method: 'POST',
    path: '/{{ENTITY_NAME}}',
    body: Type.Object({
        name: Type.String({ minLength: 1 }),
        description: Type.Optional(Type.String()),
    }),
    responses: {
        201: Type.Object({
            id: Type.Number(),
            name: Type.String(),
            description: Type.Union([Type.String(), Type.Null()]),
            createdAt: Type.String(),
            updatedAt: Type.String(),
        }),
    },
});