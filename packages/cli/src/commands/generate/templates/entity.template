/**
 * {{PASCAL_NAME}} Entity
 *
 * Auto-generated by SPFN CLI
 */

import { index, serial, text, timestamp } from 'drizzle-orm/pg-core';
import { createFunctionSchema } from '@spfn/core/db';

// Create isolated schema for @spfn/{{FN_NAME}}
const schema = createFunctionSchema('@spfn/{{FN_NAME}}');

export const {{ENTITY_NAME}} = schema.table('{{TABLE_NAME}}', {
    // Primary Key
    id: serial('id').primaryKey(),

    // Fields
    name: text('name').notNull(),
    description: text('description'),

    // Timestamps
    createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
    updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
}, (table) => [
    // Indexes
    index('{{SCHEMA_NAME}}_{{TABLE_NAME}}_name_idx').on(table.name),
]);

// Type inference
export type {{PASCAL_NAME}} = typeof {{ENTITY_NAME}}.$inferSelect;
export type New{{PASCAL_NAME}} = typeof {{ENTITY_NAME}}.$inferInsert;